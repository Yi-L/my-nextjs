export default function Solt() {
  return (
    <div className="line-clamp-2">
      <h1>@solt-----并行路由, 刷新找不到页面进入default, default 一般是null, 用于拦截路由</h1>
      <p>拦截路由允许你从当前布局内应用的其他部分加载路由。当你想要显示路由内容而不需要用户切换到不同的上下文时，此路由范例非常有用。</p>
    </div>
  );
}
